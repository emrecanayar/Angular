import { CustomArrayForDirectiveDirective } from './custom-array-for-directive.directive';

describe('CustomArrayForDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomArrayForDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
