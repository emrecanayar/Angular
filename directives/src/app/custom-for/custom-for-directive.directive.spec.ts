import { CustomForDirectiveDirective } from './custom-for-directive.directive';

describe('CustomForDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomForDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
